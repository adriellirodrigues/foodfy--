{% extends "layout.njk" %}

{% block content %}

    <div class="banner">
        <div class="banner-title">
        <h1>As melhores receitas</h1>
        <p>Aprenda a construir os melhores pratos com receitas criadas
            por profissionais do mundo inteiro.
        </p>
    </div>
        <img src="assets/chef.png" alt="Imagem chef de cozinha">
    </div>

    <div class="top-view-recipes">
        <h1>Mais acessadas</h1>
       <div class="cards">
               
            {# {%  set maxItems = 6    %}
            {% for item in items  %}
            {% if item <= maxItems  %} #}

            
            {# {% set length = item if items <= 6 else 6%} #}
            {% for item in items %}

          
                    <div id="{{item.id}}" class="card">
                        <div class="image__card">
                            <img src="{{item.image}}" alt="Imagem de {{item.title}}">
                        </div>
                        <div class="title__card">
                            <h4>{{item.title}}</h4>
                        </div>
                        <div class="author__recipe">
                            <p>{{item.author}}</p>
                        </div>
                    </div>

           {%  endfor %}

    </div>
</div>

{% endblock %}



